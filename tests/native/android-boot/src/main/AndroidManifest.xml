<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Intentionally no permissions, services, receivers, account or production code. -->
    <application android:label="LETSCUBE Boot QA" android:allowBackup="false"
        android:usesCleartextTraffic="false" android:theme="@android:style/Theme.Material.Light.NoActionBar">
        <activity android:name=".BootActivity" android:exported="false" />
    </application>
    <instrumentation android:name=".BootInstrumentation" android:targetPackage="qa.letscube.boot"
        android:functionalTest="true" android:label="Offline boot lifecycle proof" />
</manifest>
