<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme.
         DayNight, not Light. Android decides whether an app is "dark" from its
         theme, and the WebView passes that on to `prefers-color-scheme`. With a
         Light parent the platform reported the app as light no matter what the
         phone was set to: measured on two Android 15 phones with night mode on,
         the page saw `prefers-color-scheme: dark` as false and LETSCUBE
         rendered light on a dark phone. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
        <!-- Under gesture navigation Android draws its own bar at the bottom of
             the screen, and its default colour showed as a grey strip below the
             app. Transparent lets the app's own background run to the edge; the
             gesture handle still draws over it. -->
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
    </style>


    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="android:background">@drawable/splash</item>
        <!-- Named explicitly so the launch screen hands over to the DayNight
             theme rather than to whatever the platform picks. -->
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
    </style>
</resources>