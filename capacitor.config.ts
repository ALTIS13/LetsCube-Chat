import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.kub.messenger",
  appName: "KUB Messenger",
  webDir: "artifacts/kub/dist/public",
};

export default config;
