# Android 0.1.8 build 9 Stable release

Date: 2026-09-23. Source commit: `02441b29cbc4c0830bbe5a5b203e87e50ddba1e6` on `main`.
The owner explicitly authorized a new release without an attached physical
official-GMS handset. This is an APK catalog release, not a Play submission.

## Gates and result

- Local Firebase client configuration and signing inputs were present and
  ignored by Git; their values and registration tokens were not printed.
- `pnpm.cmd typecheck`: exit 0.
- Focused Android, push, notification, and catalog unit suite: 124 passed,
  1 environment-specific skip, 0 failed.
- `pnpm.cmd android:build:production:release`: exit 0. The production web bundle
  was rebuilt and synced into Capacitor. The signed APK is `0.1.8/9`,
  7,502,015 bytes, SHA-256
  `3954291616f5146acd02fdcae0651b6ec5baf5e7b9b66d59c7f4e4622ebe9cc4`.
  An AAB was produced locally but not distributed.
- `pnpm.cmd android:verify:release -- android/app/build/outputs/apk/release/app-release.apk`:
  exit 0. It checked signing, release metadata, package/exported components,
  permissions, Firebase initialization resources and Asset Links certificate.
- Downloaded the previous public `0.1.7/8` APK; its SHA-256 matched the old
  Stable manifest, and its signing certificate matched the new APK.
- Installed the signed APK on the Google Play Android 14/API 34 AVD in a
  `-read-only` overlay. It reported `com.kub.messenger` `0.1.8/9`, focused
  `MainActivity`, and rendered a usable guest login screen without a connection
  error. A guest screenshot was inspected. The AVD was shut down; the physical
  Realme debug installation was not replaced.
- Production-bundle Playwright Android auth/offline layout suite on desktop and
  mobile projects: 16 passed, 0 failed. The temporary preview server stopped.
- The previous Stable manifest was copied byte-identically to
  `/srv/letscube/backups/android-stable-pre-0.1.8-build9-20260923.json` before
  publication. The uploaded APK hash, publisher Bash syntax and highlights JSON
  passed server-side checks.
- Server-side publisher created the immutable `0.1.8` artifact and atomically
  changed Stable. Public HTTPS manifest reports `0.1.8/9`, three highlights,
  `mandatory=false`, and the expected size/hash. The downloaded public APK
  passed the same release verifier: exit 0.

Download: https://api.letscube.ru/releases/files/android/0.1.8/letscube-0.1.8.apk

## Remaining limits

The signed APK has not received a live authenticated FCM push in this pass.
Earlier `0.1.8/9` **debug** builds passed live registration, background and
normally killed delivery, OS-card tap to an exact message on Google Play API
33/34 AVDs, and microG Realme delivery/read-sync. Those results do not prove
signed-release delivery, physical official-GMS behavior, long-session reliability,
or the wider Android device matrix. In-app notifications remain the source of
truth. No database migration, Play release, AAB publication, iOS/PWA change, or
forced update was made.

Rollback, if required: verify the saved `0.1.7/8` manifest and existing
immutable `0.1.7` APK, then restore that manifest atomically under the release
catalog publish lock. Do not overwrite either versioned APK.
