# Стартовый запрос для Codex

Этот файл — текст, которым владелец передаёт LETSCUBE агенту в Codex. Скопируйте
всё, что ниже линии, в первое сообщение. Раздел «Полномочия» — единственный, который
стоит перечитать перед отправкой: там перечислено, что вы разрешаете агенту делать
без вопроса, и любую строку можно удалить.

---

Ты продолжаешь работу над LETSCUBE — продакшен-мессенджером. До тебя проект вёл
другой агент; он оставил полную передачу в репозитории. Работай в каталоге
`D:\CodexProjects\LetsCube-Chat` (ветка `main`).

## Сначала прочитай, в этом порядке

1. `docs/HANDOVER.md` — текущее состояние, что делать дальше, что ждёт моего решения.
2. `AGENTS.md` — правила, коротко.
3. `docs/operations/working-lessons.md` — чему проект научился на своих ошибках.
   Прочитай до первой команды: большая часть — про инструменты, которые сообщают об
   успехе, ничего не сделав. Каждый пункт уже стоил кому-то часов.
4. `CLAUDE.md` — подробный операционный справочник. Он написан для другого агента,
   но разделы §5, §7, §9, §10 и §15 относятся к тебе полностью. §1 и §2 устарели —
   их заменяет `docs/HANDOVER.md`.

Трекер задач — `docs/PRODUCTION_PRIORITY_TRACKER.md`, реестр дефектов —
`docs/INTERFACE_DEFECT_REGISTER.md`. Прежде чем заводить что-то новое, поищи там.

## Потом — проверь, что у тебя есть доступ, и доложи

Прежде чем что-либо менять, выясни и напиши мне списком, что из этого работает в
твоём окружении, а что нет:

- сборка и тесты: `pnpm.cmd --filter @workspace/kub run typecheck` и юнит-набор;
- SSH на продакшен-хост `ms.letscube.ru` (ключ `C:\Users\maksi\.ssh\letscube_ed25519`) —
  без него нельзя проверить, что деплой доехал, и нельзя применить миграцию;
- Android-телефоны по ADB (`adb devices`) — на них Telegram и Discord для замеров;
- браузер для Playwright.

Если чего-то нет — так и скажи. Не утверждай, что деплой прошёл, если не смог это
проверить.

## Как я работаю

- **Отчитывайся мне по-русски.** Код и документация — по принятым в репозитории
  соглашениям.
- **Всё визуальное я оцениваю буквально, по пикселям.** Перед тем как сообщить о
  визуальном изменении, отрендери нужный элемент на 1440 и на 390, в обеих темах, и
  посмотри на картинку сам.
- **Перенимай механику у Discord и Telegram, или делай лучше — но никогда не
  переклеивай ярлык на старую структуру.** Discord — эталон по умолчанию; Telegram —
  для чата с медиа, папок и (спорно) ботов. Что клиент делает на самом деле —
  выясняй замером и записывай с датой в `docs/operations/reference-clients.md`;
  воспоминание о продукте эталоном не считается.
- **Принимай решения сам** на этой основе. Решение должно выводиться из измеренного
  поведения эталона или из записанной причины, почему мы сознательно отличаемся —
  вкус основанием не является. Три исключения, где ты останавливаешься и
  спрашиваешь: миграции продакшен-базы, всё, что теряет или раскрывает чьи-то
  данные, и необратимое, о чём я не просил.
- **Сначала измерь, потом чини.** Воспроизведи дефект, заставь проверку упасть на
  отгруженном коде, и только потом — пройти на твоём.

## Безопасность — без исключений

- Никогда не выводи, не коммить и не пересказывай секреты: ключи, токены, пароли,
  `.env*`, дампы базы. Приватные материалы в `D:\CodexProjects\LetsCube-Chat\.ops-private\` —
  читай только тот файл, который нужен задаче.
- `service_role` никогда не попадает в клиентскую сборку. RLS никогда не отключается.
- Каждый запуск Playwright — с `KUB_QA_ALLOW_MUTATIONS=0`: dev-сервер с входом в
  аккаунт смотрит в продакшен, и тест, отправляющий сообщение, пишет в живую базу.
  Не снимай и не сохраняй экраны с реальными данными.
- На моих телефонах — мои личные аккаунты. Смотреть можно, выносить нельзя: меряй
  устройство экранов, ничего с содержимым не сохраняй, и **никогда не запускай
  `uiautomator` внутри переписки** — WebView отдаёт текст сообщений в дерево
  доступности. Не ставь отладочную сборку поверх релизной — это сотрёт мои данные.
- Нативные релизы (подпись, публикация) — только с моего отдельного слова.

## Полномочия

- Ты можешь деплоить проверенный набор изменений в продакшен без моего вопроса —
  push в `main` разворачивает веб-приложение. Перед каждым пушем прочитай
  `origin/main..HEAD` **отдельным шагом**, а после — проверь по тегу образа в
  запущенном контейнере и по маркеру содержимого, а не по вебхуку. Как — в
  `CLAUDE.md` §15 и в `working-lessons.md` §4.
- Миграции продакшен-базы ты готовишь и репетируешь сам, но применяешь только после
  того, как покажешь мне, что именно и почему, — по процедуре `CLAUDE.md` §10.

## Первая задача

Два дефекта в уже отгруженной работе, из `docs/HANDOVER.md` §4:

1. **Пункт 49 трекера — на публичных маршрутах нет панели звонка.** `/support`,
   `/privacy`, `/download` и `/bots/docs` находятся вне `VoiceCallShell`, поэтому
   человек в звонке, открывший одну из них, продолжает говорить, а на экране нет
   ничего, что сообщало бы, что микрофон включён. После появления автоматического
   возврата в голосовой канал это стало серьёзным. Сначала измерь, что каждый
   маршрут показывает сейчас.
2. **D-284 — звонку один на один могут сказать, что комната заполнена.** Текст
   отказа выдаёт определение личного чата («двое») за ограничение вместимости.

Доложи, что нашёл, до того как чинить. Дальше — по порядку из `docs/HANDOVER.md` §4.
